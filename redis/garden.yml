kind: Module
description: Redis service for queueing votes before they are aggregated
type: container
name: redis
image: redis:alpine
services:
  - name: redis
    command: [/bin/sh, -c]
    args: ["redis-server"]
    ports:
      - name: redis
        protocol: TCP
        containerPort: 6379
